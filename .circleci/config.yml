version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
#      - run: 
#          name: "echo an env var that is part of our project"
#          command: |
#            echo $SECRET_KEY # this env var must be swt within the project
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run:
          command: |
            python manage.py test
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
